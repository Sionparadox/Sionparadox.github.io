export const metadata = {
  title: "JavaScript Promise 완벽 가이드",
  date: "2024-03-16",
  tags: ["JavaScript", "비동기", "프론트엔드"],
  description: "JavaScript의 Promise 개념과 실제 활용 방법을 자세히 알아봅니다. (임시파일)"
};

# JavaScript Promise 완벽 가이드

Promise는 JavaScript에서 비동기 작업을 처리하는 핵심 개념입니다. 이 글에서는 Promise의 기본 개념부터 고급 사용법까지 알아보겠습니다.

## Promise 기본 개념

Promise는 비동기 작업의 최종 완료(또는 실패)와 그 결과값을 나타내는 객체입니다:

```javascript
const promise = new Promise((resolve, reject) => {
  // 비동기 작업
  if (성공) {
    resolve(결과);
  } else {
    reject(에러);
  }
});
```

## Promise 체이닝

여러 비동기 작업을 순차적으로 처리할 수 있습니다:

```javascript
fetch('/api/data')
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error(error));
```

## async/await

Promise를 더 쉽게 사용할 수 있는 문법입니다:

```javascript
async function getData() {
  try {
    const response = await fetch('/api/data');
    const data = await response.json();
    return data;
  } catch (error) {
    console.error(error);
  }
}
```

## Promise.all과 Promise.race

여러 Promise를 병렬로 처리할 수 있습니다:

```javascript
// 모든 Promise가 완료될 때까지 대기
Promise.all([promise1, promise2, promise3])
  .then(results => console.log(results));

// 가장 먼저 완료되는 Promise의 결과 반환
Promise.race([promise1, promise2])
  .then(result => console.log(result));
```

## 마치며

Promise는 현대 JavaScript 개발에서 필수적인 개념입니다. 
이 글에서 배운 내용을 바탕으로 더 효율적인 비동기 코드를 작성해보세요. 