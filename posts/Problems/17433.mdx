export const metadata = {
  title: '[Gold V] 신비로운 수 - 17433',
  date: '2026-02-23 08:20:54',
  tags: ['수학', '정수론', '유클리드 호제법'],
  description: '[Gold V] 신비로운 수 - 17433',
};

# [Gold V] 신비로운 수 - 17433 

[문제 링크](https://www.acmicpc.net/problem/17433) 

### 성능 요약

메모리: 33432 KB, 시간: 44 ms

### 분류

수학, 정수론, 유클리드 호제법

### 제출 일자

2026년 2월 23일 17:20:38

### 문제 설명

0이 아닌 정수 N개가 주어졌을 때, 0이 아닌 정수 M이 다음 성질을 만족하면 M은 N개의 정수에 대해 신비로운 수라고 한다.

	N개의 정수를 M으로 나눈 나머지가 모두 같다.

임의의 N개의 정수에 대해 신비로운 수는 적어도 하나 이상 존재한다. 예를 들어, 1은 N개의 정수와 상관없이 항상 신비로운 수이다.

N개의 수가 주어졌을 때, N개의 정수에 대해 신비로운 수 중에서 가장 큰 수를 구해보자.

### 입력 

 첫째 줄에 테스트 케이스의 개수 T가 주어진다. 각 테스트 케이스는 두 줄로 이루어져 있고, 첫째 줄에 N, 둘째 줄에 N개의 정수가 주어진다.

### 출력 

 각각의 테스트 케이스마다 N개의 정수에 대한 가장 큰 신비로운 수를 출력한다. 만약 신비로운 수가 무한대로 발산하는 경우 "INFINITY"를 출력한다.

### 풀이
```python
import sys
input = sys.stdin.readline

def GCD(x, y):
    if y == 0:
        return x
    return GCD(y, x%y)

T = int(input())

for t in range(T):
    N = int(input())
    arr = list(map(int, input().split()))
    diff = []
    for i in range(1,N):
        diff.append(arr[i]-arr[0])
    if all(d == 0 for d in diff):
        print("INFINITY")
        continue
    
    answer = diff[0]
    for d in diff[1:]:
        answer = GCD(answer, d)
    print(abs(answer))

'''
모든 수가 같을 때에만 발산
각 수를 바꿨을 떄
ak+m, bk+m, ck+m 같은 형식이 되면 1이 아닌 M이 존재
-> bk-ak = xk
-> ck-ak = yk
각 수의 차이가 가지는 최대공약수가 답
x%y = r
GCD(x, y) = GCD(y, r)

'''
```