export const metadata = {
  title: '[Silver I] 행렬 - 1080',
  date: '2026-02-20 07:56:41',
  tags: ['그리디 알고리즘'],
  description: '[Silver I] 행렬 - 1080',
};

# [Silver I] 행렬 - 1080 

[문제 링크](https://www.acmicpc.net/problem/1080) 

### 성능 요약

메모리: 32412 KB, 시간: 40 ms

### 분류

그리디 알고리즘

### 제출 일자

2026년 2월 20일 16:56:19

### 문제 설명

0과 1로만 이루어진 행렬 A와 행렬 B가 있다. 이때, 행렬 A를 행렬 B로 바꾸는데 필요한 연산의 횟수의 최솟값을 구하는 프로그램을 작성하시오.

행렬을 변환하는 연산은 어떤 3×3크기의 부분 행렬에 있는 모든 원소를 뒤집는 것이다. (0 → 1, 1 → 0)

### 입력 

 첫째 줄에 행렬의 크기 N M이 주어진다. N과 M은 50보다 작거나 같은 자연수이다. 둘째 줄부터 N개의 줄에는 행렬 A가 주어지고, 그 다음줄부터 N개의 줄에는 행렬 B가 주어진다.

### 출력 

 첫째 줄에 문제의 정답을 출력한다. 만약 A를 B로 바꿀 수 없다면 -1을 출력한다.

### 풀이
```python
import sys
input = sys.stdin.readline

N, M = map(int, input().split())
matrixA = []
matrixB = []
for _ in range(N):
    _input = list(map(int ,list(input().rstrip())))
    matrixA.append(_input)

for _ in range(N):
    _input = list(map(int ,list(input().rstrip())))
    matrixB.append(_input)

def flip(r, c):
    for nr in range(r, r+3):
        for nc in range(c, c+3):
            matrixA[nr][nc] = 1-matrixA[nr][nc]

answer = 0
for r in range(N-2):
    for c in range(M-2):
        if matrixA[r][c] != matrixB[r][c]:
            flip(r, c)
            answer += 1

for r in range(N):
    for c in range(M):
        if matrixA[r][c] != matrixB[r][c]:
            answer = -1
            break
    if answer == -1:
        break

print(answer)
```