export const metadata = {
  title: 'CSS Grid Layout 마스터하기',
  date: '2024-03-15',
  tags: ['CSS', 'Grid', '프론트엔드'],
  description: 'CSS Grid Layout의 기본 개념부터 실전 활용까지 상세히 알아봅니다.(임시파일)',
};

# CSS Grid Layout 마스터하기

CSS Grid는 2차원 레이아웃을 만들기 위한 강력한 도구입니다. 이 글에서는 Grid의 기본 개념부터 실전 활용까지 알아보겠습니다.

## Grid 기본 개념

Grid는 행과 열을 가진 격자 형태의 레이아웃을 만듭니다:

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: auto;
  gap: 20px;
}
```

## Grid 아이템 배치

Grid 아이템을 원하는 위치에 배치할 수 있습니다:

```css
.grid-item {
  grid-column: 1 / 3; /* 1번 열부터 3번 열 전까지 */
  grid-row: 2 / 4; /* 2번 행부터 4번 행 전까지 */
}
```

## 반응형 Grid

미디어 쿼리를 활용한 반응형 Grid 레이아웃:

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
}
```

## Grid 영역 이름 지정

영역에 이름을 지정하여 더 직관적인 레이아웃 구성:

```css
.grid-container {
  display: grid;
  grid-template-areas:
    'header header header'
    'sidebar main main'
    'footer footer footer';
}

.header {
  grid-area: header;
}
.sidebar {
  grid-area: sidebar;
}
.main {
  grid-area: main;
}
.footer {
  grid-area: footer;
}
```

## 마치며

CSS Grid는 현대적인 웹 레이아웃을 만드는 데 필수적인 도구입니다.
이 글에서 배운 내용을 바탕으로 더 복잡하고 유연한 레이아웃을 구현해보세요.
